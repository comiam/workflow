import lang/vmruntime;
import lang/grammar/gram_engine;
import runtime;
import sys/system;


main() -> void {
	alg = strReplace(readUntil("---"), "---", "");
	//println(s2pseq(alg));

	prog = compile(s2pseq(alg));
	printVM(prog);

	a = execVM(prog);
}